# Current, supported http_forwarder subtree. Test destination; no live credential.
ingress:
  endpoint: 127.0.0.1:18126
  compression_algorithms: []
egress:
  endpoint: http://127.0.0.1:18127
  headers:
    DD-API-KEY: research-only-key
    X-Datadog-Additional-Tags: host:research-host,default_env:research
  timeout: 5s
